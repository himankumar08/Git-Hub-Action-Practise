# GIT-HUB-Actions

## Cocnept

## 1. Workflow
  To craete a github action workflow we need to create a folder `.github/workflows`
  Inside the `.github/workflows` we create the yml file where our actual workflows is being coded in YAML language.
## 2. jobs 
 It is where we define what the workflow is about, what are the things workflow is actually doing.
 Inside this we wirte ``Steps``. Where we pass the commands.
 ## 3. action 
  It is where we pass the command to be automated. like pull push and all.
